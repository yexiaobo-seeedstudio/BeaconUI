<?xml version="1.0" encoding="utf-8"?>
<selector xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- For button Press state -->
    <item android:state_pressed="true"><shape>
            <gradient android:angle="270" android:endColor="#2f6699" android:startColor="#449def" />

            <stroke android:width="1dp" android:color="#2f6699" />

            <corners android:radius="3dp" />

            <padding android:bottom="6dp" android:left="6dp" android:right="6dp" android:top="6dp" />
        </shape></item>
    <!-- For default state -->
    <item><shape>

            <!-- <gradient android:angle="270" android:endColor="#bb6008" android:startColor="#f3ae1b" /> -->
            <!-- <stroke android:width="1dp" android:color="#bb6008" /> -->
            <stroke android:width="2dp" android:color="#2f6699" />

            <solid android:color="#00ffffff" />

            <corners android:radius="4dp" />

            <padding android:bottom="6dp" android:left="6dp" android:right="6dp" android:top="6dp" />
        </shape></item>

    <!--
gradient 主体渐变 startColor开始颜色，endColor结束颜色 ，angle开始渐变的角度（值只能为90的倍数，0时为左到右渐变，90时为下到上渐变，依次逆时针类推）
stroke 边框 width 边框宽度，color 边框颜色
corners 圆角 radius 半径，0为直角
padding text值的相对位置
    -->

</selector>